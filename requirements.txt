# Core
requests==2.32.5
python-dotenv==1.2.2

# Telegram bot (long-polling, no inbound ports needed)
python-telegram-bot==22.8

# Flight data (free provider - Google Flights via Protobuf)
fast-flights==3.0.2

# Local REST API server (optional, for debugging/dashboards)
fastapi==0.139.0
uvicorn[standard]==0.49.0

# Tests
pytest==9.1.1
