# Shell scripts and the systemd unit must keep Unix (LF) line endings so they
# run correctly on the Linux VPS, even though this repo is edited on Windows.
*.sh            text eol=lf
*.service       text eol=lf
deploy/setup.sh  text eol=lf
deploy/update.sh text eol=lf
